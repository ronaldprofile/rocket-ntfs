import { styled } from "../../styles";
import { backgroundWaves } from "../../assets";

export const Container = styled("div", {
  width: "100%",
  height: "100%",
  background: `url(${backgroundWaves})`
});
